{
    "extends": "../tsconfig.build.json",
    "compilerOptions": {
        "outDir": ".",
        "rootDir": ".",
        "types": ["node"],
        "paths": {
            "@cmmv/server": ["../server"]
        }
    },
    "exclude": ["node_modules", "dist"],
    "references": [{ "path": "../server/tsconfig.build.json" }]
}
