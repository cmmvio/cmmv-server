{
    "extends": "../tsconfig.build.json",
    "compilerOptions": {
        "outDir": ".",
        "rootDir": ".",
        "types": ["node", "mocha"],
        "paths": {
            "@cmmv/server": ["../server"]
        }
    },
    "exclude": ["node_modules", "dist"],
    "references": [
        { "path": "../server/tsconfig.build.json" }
    ]
}
